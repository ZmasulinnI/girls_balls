/*#include "Libraries.h"
#include "Function_Prototypes.h"

int Find_first_pos(double* arr, int n)
{
    int first_pos = -1;
    int last_pos = -1;
    for (int i = 0; i < n; i++)
    {
        if (arr[i] > 0)
        {
            if (first_pos == -1)
            {
                first_pos = i;
            }
            else if (last_pos == -1)
            {
                last_pos = i;
                break;
            }
        }
    }
    return last_pos;
}*/
